<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="6dp">

    <!-- 刷新按钮 - 左上角 -->
    <ImageButton
        android:id="@+id/btn_refresh"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_margin="5dp"
        android:background="@drawable/widget_button_background"
        android:src="@drawable/ic_refresh"
        android:scaleType="centerInside"
        android:contentDescription="刷新数据" />

    <!-- 主要内容区域 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:layout_marginTop="0dp">

    <!-- 左侧：宠物图像和名称区域 -->
    <LinearLayout
        android:layout_width="100dp"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="top"
        android:layout_marginTop="4dp"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="0dp">
        
        <!-- 宠物图像（按位图原尺寸显示，避免系统再次缩放导致模糊） -->
        <ImageView
            android:id="@+id/pet_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="center"
            android:src="@drawable/pet_catbrown_sit_1"
            android:contentDescription="Pet Image" />
            
        <!-- 宠物名称 - 显示在图片正下方（使用ImageView显示自定义字体位图） -->
        <ImageView
            android:id="@+id/pet_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:scaleType="center"
            android:layout_marginTop="-2dp"
            android:layout_marginStart="8dp"
            android:contentDescription="宠物名字" />
            
        <!-- 宠物状态 - 显示在名称下方（使用ImageView显示自定义字体位图） -->
        <ImageView
            android:id="@+id/pet_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:scaleType="center"
            android:layout_marginTop="6dp"
            android:layout_marginStart="16dp"
            android:contentDescription="宠物状态"
            android:visibility="gone" />
            
    </LinearLayout>

    <!-- 中间：宠物信息区域 -->
    <LinearLayout
        android:layout_width="80dp"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="top"
        android:layout_marginStart="4dp"
        android:layout_marginTop="26dp"
        android:layout_marginEnd="0dp">
        

        <!-- 年龄（使用ImageView显示自定义字体位图） -->
        <ImageView
            android:id="@+id/pet_age"
            android:layout_width="match_parent"
            android:layout_height="16dp"
            android:scaleType="fitStart"
            android:layout_marginBottom="20dp"
            android:contentDescription="年龄" />

        <!-- 精力（使用ImageView显示自定义字体位图） -->yij
        <ImageView
            android:id="@+id/pet_energy"
            android:layout_width="match_parent"
            android:layout_height="16dp"
            android:scaleType="fitStart"
            android:layout_marginBottom="20dp"
            android:contentDescription="精力" />

        <!-- 饱食度（使用ImageView显示自定义字体位图） -->
        <ImageView
            android:id="@+id/pet_satiety"
            android:layout_width="match_parent"
            android:layout_height="16dp"
            android:scaleType="fitStart"
            android:contentDescription="饱食度" />

    </LinearLayout>

    <!-- 右侧：功能按钮区域 -->
    <LinearLayout
        android:layout_width="80dp"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="top"
        android:layout_marginTop="18dp">

        <!-- 坐下按钮 -->
        <ImageButton
            android:id="@+id/btn_sit"
            android:layout_width="80dp"
            android:layout_height="32dp"
            android:background="@drawable/widget_button_background_highlighted"
            android:scaleType="center"
            android:layout_marginBottom="12dp"
            android:minHeight="0dp"
            android:minWidth="0dp"
            android:contentDescription="坐下" />

        <!-- 左右看按钮 -->
        <ImageButton
            android:id="@+id/btn_visit"
            android:layout_width="80dp"
            android:layout_height="32dp"
            android:background="@drawable/widget_button_background"
            android:scaleType="center"
            android:layout_marginBottom="12dp"
            android:minHeight="0dp"
            android:minWidth="0dp"
            android:contentDescription="左右看" />

        <!-- 跑步按钮 -->
        <ImageButton
            android:id="@+id/btn_run"
            android:layout_width="80dp"
            android:layout_height="32dp"
            android:background="@drawable/widget_button_background"
            android:scaleType="center"
            android:minHeight="0dp"
            android:minWidth="0dp"
            android:contentDescription="跑步" />

            </LinearLayout>

    </LinearLayout>

</RelativeLayout>